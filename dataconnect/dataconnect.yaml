specVersion: "v1beta"
serviceId: "finalyearprojecttest"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "finalyearprojecttest-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
